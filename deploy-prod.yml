- name: Deploy to Prod Server
  hosts: prod
  become: yes

  tasks:
    - name: Remove old container
      shell: docker rm -f webapp || true

    - name: Run production container
      shell: docker run -d -p 80:80 --name webapp hshar/webapp
